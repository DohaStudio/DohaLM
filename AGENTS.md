# DohaLM 저장소 작업 규칙

이 파일은 저장소 전체에 적용되는 전역 규칙이다. 작업 경로에 하위 `AGENTS.md`가 있으면 전역 규칙을 유지하면서 해당 영역의 구체적인 규칙을 추가 적용한다.

## 1. 규칙 우선순위

다음 순서로 적용한다.

1. 사용자의 현재 요청
2. 저장소 루트 `AGENTS.md`
3. 작업 경로에 가장 가까운 하위 `AGENTS.md`
4. 상태가 `approved`인 ADR
5. 관련 설계 문서
6. 일반 프로젝트 관례

- 하위 `AGENTS.md`는 상위 규칙을 무효화하지 않고 해당 영역의 제한과 검증을 구체화한다.
- 확정된 ADR과 일반 문서가 충돌하면 현재 상태가 `approved`인 최신 ADR을 우선하고 충돌 사실을 완료 보고에 기록한다.
- 상위 안전 규칙이나 사용자의 금지 사항을 하위 규칙으로 완화하지 않는다.

## 2. 작업 전 필수 절차

1. 저장소 루트와 현재 경로를 확인한다.
2. 현재 브랜치를 확인한다.
3. `git status`로 작업 트리 상태를 확인한다.
4. 루트 `AGENTS.md`를 읽는다.
5. 작업 경로에 적용되는 하위 `AGENTS.md`를 읽는다.
6. 관련 README를 읽는다.
7. 관련 설계 문서를 읽는다.
8. 관련 ADR을 읽는다.
9. 기존 구현과 테스트를 검색한다.
10. [Definition of Ready](docs/31-definition-of-ready.md)를 확인한다.
11. 작업 범위와 제외 범위, 생성·수정·금지 파일을 정리한다.

## 3. Clean worktree 정책

- 미커밋 변경을 `사용자가 이미 알려준 의도적인 변경`, `현재 작업이 만든 변경`, `출처를 확인할 수 없는 기존 변경`으로 구분한다.
- 출처를 확인할 수 없는 기존 변경이 있으면 해당 파일을 덮어쓰거나 되돌리지 않고 작업을 중단해 변경 파일과 차단 원인을 보고한다.
- 사용자가 현재 작업에서 함께 수정하라고 명시한 파일은 기존 변경의 성격과 겹침을 확인한 뒤 예외로 처리할 수 있다.
- 현재 작업이 만든 변경만 요청 범위 안에서 정리할 수 있으며 사용자 변경을 자동 복원하거나 폐기하지 않는다.

## 4. 작업 유형

작업 유형은 `documentation`, `code`, `test`, `data`, `experiment`, `configuration`, `infrastructure`, `release`로 구분한다. 유형마다 필요한 Ready 조건과 검증이 다르다.

- `documentation`: 소스·설정·데이터를 수정하지 않고 문서 링크·수치·상태·인덱스를 검증한다.
- `code`: 관련 설계와 ADR을 확인하고 unit·smoke·regression 테스트 및 문서 영향을 검토한다.
- `test`: 요구사항과 실패 조건을 먼저 확인하고 기존 테스트를 삭제하거나 약화하지 않는다.
- `data`: 승인·라이선스·계보·개인정보·누수 조건을 우선 확인한다.
- `experiment`: Ready와 Gate를 확인하고 사용자의 명시적 승인 없이 장시간 학습을 시작하지 않는다.
- `configuration`: 설정 우선순위와 resolved config 기록, 비밀 분리를 검증한다.
- `infrastructure`: 배포·CI·외부 시스템 변경 권한과 rollback 범위를 먼저 확인한다.
- `release`: Done, 산출물, 라이선스, 보안, 버전 정책과 사용자 승인을 확인한다.

## 5. 범위 제한과 구현 원칙

- 요청받은 파일과 직접 필요한 파일만 최소 범위로 수정한다.
- 요청 없는 기능, 리팩터링, 저장소 구조 변경을 하지 않는다.
- 확정되지 않은 설정을 임의로 결정하지 않고 `[검증 필요]`로 유지한다.
- `DohaLM-Small` 상세 구조는 Tiny 실측과 후속 결정 전 임의로 확정하지 않는다.
- FastAPI·Next.js 등 서비스 기능을 `DohaLM-Tiny` 학습·평가 검증보다 우선 구현하지 않는다.
- 기존 테스트를 삭제·우회·약화하지 않고 실패 상태를 숨기지 않는다.
- 빈 예외 처리, 광범위한 오류 무시, 근거 없는 fallback을 추가하지 않는다.
- 기준 하드웨어는 단일 `RTX 3060 Ti 8GB`이며 멀티 GPU를 기본 전제로 하지 않는다.
- 핵심 모델은 PyTorch로 직접 구현하며 완성형 GPT 모델 클래스로 대체하지 않는다.
- 모델·데이터·학습·평가 설정은 단일 설정 체계에서 관리하고 코드에 분산 하드코딩하지 않는다.

## 6. 문서 상태 변경 권한

- `planned → draft`: 실제 초안이 작성된 경우에만 가능하다.
- `draft → review`: 필수 항목과 계획된 문서 검증 결과가 작성된 경우에만 가능하다.
- `review → approved`: 사용자 승인 또는 현재 요청의 명시적 승인 지시가 있을 때만 가능하다.
- `approved → implemented`: 코드·설정·테스트 반영과 검증이 모두 끝난 경우에만 가능하다.
- `deprecated`: 후속 ADR 또는 사용자의 명시적 결정이 있을 때만 가능하다.
- Codex가 문서를 작성했다는 이유만으로 `approved` 또는 `implemented`로 변경하지 않는다.

## 7. ADR 검토 조건

다음 사항을 새로 결정하거나 기존 승인 정책에서 변경할 때 ADR을 검토한다.

- 모델 구조와 파라미터 수
- Vocabulary와 Context Length
- Normalization과 positional representation
- Weight tying
- 데이터 거버넌스
- 평가 방식
- Checkpoint schema
- 공개 API
- 저장소 핵심 구조
- 기존 승인 정책

사소한 오탈자 수정, 링크 교정, 의미를 바꾸지 않는 설명 보완에는 ADR을 만들지 않는다.

## 8. 테스트와 검증 정책

- 필수 테스트가 실패하면 작업·Gate·버전을 완료 처리하지 않는다.
- 실행하지 못한 테스트와 이유, 영향 범위를 완료 보고에 명시한다.
- CPU 테스트와 GPU 테스트를 구분하며 GPU 테스트를 실행하지 않았으면 실행했다고 표현하지 않는다.
- 문서 작업은 Markdown 상대 링크, 코드 블록 균형, 후행 공백, 수치·상태·상대경로 및 `git diff --check`를 검증한다.
- 코드 작업은 변경과 직접 관련된 unit·smoke·regression 테스트를 실행한다.
- 학습 작업은 단일 배치·극소량 데이터 overfit, checkpoint 저장·복원과 resume 검증을 장시간 학습보다 먼저 수행한다.
- 테스트 기준은 [테스트 전략](docs/33-test-strategy.md)과 [테스트 체크리스트](docs/18-testing-checklist.md)를 따른다.
- 완료 보고 전에 [Definition of Done](docs/32-definition-of-done.md)을 확인하고 미충족 항목을 숨기지 않는다.

## 9. Git 쓰기 정책

사용자의 현재 요청에 명시적으로 포함되지 않는 한 다음을 수행하지 않는다.

- `git add`, `git commit`, `git push`
- merge, rebase, tag, release
- branch 생성·삭제
- force push
- `git reset --hard`, `git clean`
- 변경 파일 복원 또는 사용자 변경 폐기

브랜치를 새로 만들거나 어떤 Git 쓰기 작업을 수행할 때도 사용자의 현재 요청이 필요하다. 커밋을 요청받은 경우 한국어 커밋 메시지와 논리적 변경 단위를 사용한다.

## 10. 완료 보고 형식

1. 작업 시작 상태
2. 읽은 파일
3. 변경한 파일
4. 작업 유형과 목적
5. 주요 작업 내용
6. 확정한 사항
7. 유지한 미결정 사항
8. 실행한 명령
9. 테스트 및 검증 결과
10. 문서 정합성 결과
11. 범위 밖 변경 여부
12. 실행하지 못한 검증
13. 위험 및 남은 문제
14. 현재 브랜치
15. 최종 Git 상태
16. 다음 권장 작업
