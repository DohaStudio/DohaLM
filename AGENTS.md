# 반드시 포함할 규칙
## 작업 시작 규칙
- 작업 전에 README.md와 관련 문서를 읽는다.
- 현재 브랜치를 확인한다.
- 작업 트리가 깨끗한지 확인한다.
- 요청 범위를 명확하게 식별한다.
- 관련된 기존 코드를 먼저 검색한다.
- 기존 구현을 확인하지 않고 새 구조를 만들지 않는다.

## 범위 제한 규칙
- 요청받지 않은 기능을 추가하지 않는다.
- 요청받지 않은 파일을 대규모 수정하지 않는다.
- 기존 확정 사항을 임의로 변경하지 않는다.
- 확정되지 않은 항목은 [검증 필요]로 기록한다.
- 리팩터링과 기능 구현을 한 커밋에 섞지 않는다.

## 구현 규칙
- 하드웨어는 RTX 3060 Ti 8GB를 기준으로 한다.
- 멀티 GPU를 기본 전제로 하지 않는다.
- 모델 핵심 구조는 PyTorch로 직접 구현한다.
- Hugging Face 완성형 GPT 모델을 핵심 구현으로 대체하지 않는다.
- 설정값은 코드에 직접 분산해서 작성하지 않는다.
- 모델 설정은 구성 파일과 단일 설정 객체로 관리한다.
- 재현 가능한 random seed를 사용한다.

## 검증 규칙
- 변경한 코드와 직접 관련된 테스트를 실행한다.
- 실행하지 못한 테스트는 완료 보고에 명시한다.
- OOM 가능성이 있는 학습은 최소 설정으로 먼저 검증한다.
- 작은 데이터 오버피팅 테스트를 통과하기 전 본 학습을 하지 않는다.
- 문서의 명령어가 실제로 실행 가능한지 검사한다.

## Git 규칙
- main에서 직접 개발하지 않는다.
- 기능 브랜치에서 작업한다.
- 요청 없이 push·merge·tag를 수행하지 않는다.
- 커밋 메시지 규칙을 준수한다.
- 하나의 커밋은 하나의 논리적 목적을 가진다.
- 테스트 실패 상태를 완료로 보고하지 않는다.